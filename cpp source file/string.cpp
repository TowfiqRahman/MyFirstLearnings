#include <iostream>
#include <fstream>
#include <string.h>
using namespace std;
int main(){
    char a[200],b[30],c[100];
    string c1;
    gets(a);gets(b);///fstream must be used
    strcpy(c,a); ///string.h have to be used
    puts(c);


return 0;
}
