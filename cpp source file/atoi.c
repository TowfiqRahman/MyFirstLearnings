#include <stdio.h>
#include <stdlib.h>

int main(){
    char str[10];
    int a,i=0;
    while(scanf("%s",str)!='.');
    printf("%s",str);
    a=atoi(str);
    printf("\n%d",a);
return 0;
}
